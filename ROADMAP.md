## ROADMAP 1
- [ ] Check out whether change the screen size will cause error
- [x] Implement a SceneManager using ImGui
- [ ] Write a notes on how the texture(A, B) works and how the screen texture works (the buffer)
- [ ] Handle scene switch better, clean the data or reload the data?
